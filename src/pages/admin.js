import styled from "styled-components";

const StyledAdmin = styled.div`
    text-align: center;
`;

function Admin() {
    return (
        <StyledAdmin>
            <p>Admin</p>
        </StyledAdmin>
    );
}

export default Admin;
