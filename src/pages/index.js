import styled from "styled-components";

const StyledMetrics = styled.div`
    text-align: center;
`;

function Metrics() {
    return (
        <StyledMetrics>
            <p>Metrics</p>
        </StyledMetrics>
    );
}

export default Metrics;
