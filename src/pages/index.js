import styled from "styled-components";

const StyledOverall = styled.div``;

function Overall() {
    return (
        <StyledOverall>
            Test
        </StyledOverall>
    );
}

export default Overall;
