import styled from "styled-components";

const StyledFinances = styled.div`
    text-align: center;
`;

function Finances() {
    return (
        <StyledFinances>
            <p>Finances</p>
        </StyledFinances>
    );
}

export default Finances;
